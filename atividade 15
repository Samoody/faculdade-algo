// Cabeçalho
// Autor: [Samuel]
algoritmo "Soma e Produto de Três Números"
// Início do algoritmo
inicio
    // Declaração de variáveis
    inteiro num1, num2, num3
    inteiro soma, produto

    // Entrada de dados
    escreva("Digite o primeiro número: ")
    leia(num1)
    
    escreva("Digite o segundo número: ")
    leia(num2)
    
    escreva("Digite o terceiro número: ")
    leia(num3)

    // Linha em branco após as entradas
    escreval()

    // Cálculo da soma e do produto
    soma <- num1 + num2 + num3
    produto <- num1 * num2 * num3

    // Saída de dados com linhas em branco
    escreval("A soma dos números é: ", soma)
    escreval()
    escreval("O produto dos números é: ", produto)

// Fim do algoritmo
fimalgoritmo
